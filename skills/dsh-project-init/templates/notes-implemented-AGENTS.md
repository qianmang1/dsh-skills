# AGENTS.md — 已实施的决策记录（implemented）

这些记录描述已落地的决策。规则见 .agents/notes/README.md；scripts/check.mjs 校验生命周期结构与时态。

## 与事实保持同步

路径、符号、默认值、机制随实际落地在同一变更里更新；重写过时事实（只改事实），不追加变更历史。

## 不改写决策本身

决策与理由的反转需要新记录并交叉链接；完全被取代的记录只能按 README「Supersession 检查」的合并规则删除，不得改写成本身反面。
